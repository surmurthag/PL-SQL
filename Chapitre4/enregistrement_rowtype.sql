-- enregistrement_rowtype.sql
DECLARE
-- déclaration d’une variable enregistrement 
-- de même type que la table SQL PROJET
   prj_record PROJET%ROWTYPE;

-- déclaration d’une variable enregistrement 
-- de même type que la table SQL EMPLOYE
   emp_RECORD EMPLOYE%ROWTYPE;

BEGIN

   NULL;

END;
/
